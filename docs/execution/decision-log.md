---
title: Decision log
status: active
last_updated: 2026-08-12
---

# Decision log

| ID | Date | Status | Decision | Rationale | Revisit when |
|---|---|---|---|---|---|
| D-001 | 2026-08-06 | Recommended | Use **RoleDawn** as the working brand. | Distinctive, owns the overnight mechanism, and passed preliminary exact-name/domain screens. | Legal trademark review or user research finds confusion, awkward pronunciation, or weak recall. |
| D-002 | 2026-08-06 | Accepted | Position around trust and verified outcomes, not maximum application volume. | Volume is easy to copy and amplifies ATS, support, and reputation risk. | Cohort data proves a different promise produces materially better retention and interviews. |
| D-003 | 2026-08-06 | Accepted | Target an iPhone-first active-search state; use early-career as the wedge without making age the brand. | Channel behavior and acute pain are more useful than a generational label. | Research shows another cohort has much higher urgency, willingness to pay, and fit. |
| D-004 | 2026-08-06 | Accepted | iMessage is a control surface, not the canonical UI or system of record. | Long diffs, sensitive fields, portability, and provider risk require a secure web surface. | Apple provides a suitable direct server API and users consistently prefer messaging for complex review. |
| D-005 | 2026-08-06 | Recommended | Pilot iMessage through Photon behind `ChannelAdapter`; keep web/SMS fallback. | Fastest path to a realistic alpha without making vendor semantics architectural. | Photon diligence fails platform, security, portability, or reliability requirements. |
| D-006 | 2026-08-06 | Accepted | Do not run a permanent general-purpose agent or container per user. | Durable logical state plus on-demand workers is safer, cheaper, and easier to version and observe. | A measured workload requires dedicated isolation for a regulated or enterprise tier. |
| D-007 | 2026-08-06 | Accepted | Do not use Hermes as the production control plane. | Broad tools and agent-managed memory conflict with deterministic policy, tenant isolation, and audit. | A pinned sandboxed executor beats purpose-built workers in a controlled evaluation. |
| D-008 | 2026-08-06 | Recommended | Use Temporal for one durable workflow per application. | Applications span hours or days, wait on people, and require safe retry/reconciliation. | A simpler queue implementation meets the same tested durability and recovery requirements at materially lower complexity. |
| D-009 | 2026-08-06 | Accepted | Browser automation is the universal MVP execution path; formal ATS partnerships are the moat roadmap. | Public ATS APIs expose jobs, while arbitrary candidate submissions usually require employer or partner credentials. | A verified candidate-authorized direct submission standard emerges. |
| D-010 | 2026-08-06 | Accepted | Require final-submit approval for every MVP application. | Trust, legal attestations, and unreliable terrain make silent autopilot premature. | A validated adapter and explicit bounded standing authorization meet rollout gates. |
| D-011 | 2026-08-06 | Accepted | Exact/sensitive facts come from structured, approved records; embeddings are supporting retrieval only. | Similarity search is not a safe authority for identity, dates, work authorization, or legal answers. | Never; implementation details may change, the invariant does not. |
| D-012 | 2026-08-06 | Recommended | PWA first, native mobile after the approval loop is validated. | iMessage plus responsive web tests the core behavior without duplicating product work. | Retention data shows native-only capability is the adoption bottleneck. |
| D-013 | 2026-08-06 | Hypothesis | After alpha, test $99 for 30 confirmed applications and $149 for 75; `confirmed_application` is the only billable unit. Failed, canceled, uncertain, or duplicate attempts do not count. | This keeps the experiment out of the penny-per-application race and exposes whether measured cost can support at least 65% gross margin at full allowance. | Cost, conversion, utilization, and willingness-to-pay data support a different grid. |
| D-014 | 2026-08-06 | Accepted | Gate all employer logos and outcome claims behind consent and documented evidence. | Precision is both legally prudent and a brand differentiator. | Never; presentation may change, the truth requirement does not. |
| D-015 | 2026-08-06 | Accepted | PostgreSQL owns durable domain state, Temporal owns in-flight execution history, and the append-only ledger owns consequential proof; synchronize through transactional outbox/inbox and optimistic versions. | Calling multiple stores “canonical” without ownership creates unsafe divergence near approvals and submit. | A tested alternative preserves the same transaction, replay, and repair guarantees. |
| D-016 | 2026-08-06 | Accepted | Build the night shift on an approved official-source registry with stable job identity, incremental polling, freshness/closure metrics, and source-policy controls. | Continuous discovery is a product subsystem, not a search query inside an agent prompt. | Licensed/partner feeds replace specific acquisition paths. |
| D-017 | 2026-08-06 | Accepted | Use “Nothing invented. Nothing outside your rules.” as the durable trust line; use “Nothing submitted without your approval” only while per-application approval is the actual product. | Future standing authorization would make “nothing sent unseen” false even if it remains inside explicit user rules. | Product permanently commits to pre-send visibility for every application. |
| D-018 | 2026-08-06 | Accepted | Model messaging-provider resources separately from verified user channel bindings; do not provision one iMessage line per candidate by default. | A project line or bounded pool can serve many users, while identity and consent remain internal RoleDawn records. | A provider or platform contract requires one dedicated resource per user or a tier needs isolated identity. |
| D-019 | 2026-08-06 | Accepted | Keep prompts, tool grants, model routes, and policies in centrally promoted immutable versions; expose only bounded product preferences to tenants. | Raw per-tenant agent configuration creates unreviewed authority, quality drift, and unreplayable behavior. | An enterprise control is expressible as a tested, typed policy without expanding model authority. |
| D-020 | 2026-08-06 | Accepted | Parse each public job version once, retrieve potentially eligible searches with indexed hard dimensions, run exact rules, then perform bounded reranking and draft only admitted top-K items. | This prevents a model-scored jobs-by-users cross product and reserves cost for likely queue items. | Measured retrieval quality or scale requires a different index while preserving bounded work. |
| D-021 | 2026-08-06 | Accepted | Domain records store opaque credential references; a dedicated broker owns OAuth tokens and issues narrowly scoped short-lived access to workers. | Tokens do not belong in prompts, ordinary database rows, browser logs, or tenant-editable connector configuration. | A managed connector proves an equivalent or stronger audited boundary. |
| D-022 | 2026-08-06 | Accepted | Defer Gmail OAuth until the core Career Vault → approval → submission receipt loop proves value; start with no mailbox access. | Gmail adds verification, privacy, support, and possibly restricted-scope assessment burden before the core product is validated. | Recruiter follow-up or confirmation reconciliation shows measured value that cannot be met by forwarding or user-provided messages. |
| D-023 | 2026-08-06 | Accepted | Use the day → night → dawn → day product story and a distinct midnight/coral/yellow/mint system; borrow Viktor's output-first clarity without its purple trade dress or “AI hire” framing. | The visual mechanism makes continuous cloud work legible while keeping authority and proof central to RoleDawn. | Usability or recall testing shows the narrative hides the product or harms conversion/performance. |
| D-024 | 2026-08-06 | Accepted | Treat Supabase and the Anthropic Agent SDK as benchmark candidates, not settled architecture. Keep database/auth and model runtimes behind owned contracts. | Both can accelerate delivery, but neither should silently own workflow authority, permission, or provider portability. | The alpha benchmarks, security review, and cost analysis select a vendor and record the bounded commitment. |
| D-025 | 2026-08-06 | Accepted | Adapt Clay's large-type hierarchy, tactile proof rhythm, use-case tabs, rounded feature chapters, and human-first copy grammar without copying its trade dress. Ship Manrope through `next/font` behind a family token; do not ship Roobert without the required commercial license. | The reference provides a strong page rhythm, while RoleDawn's day-night-dawn story, palette, original object world, provenance, single-use approval, and receipt contract preserve an independent identity and truthful product promise. | A valid Roobert license and a readability/conversion test justify a type-token swap, or accessibility and conversion tests show the composition should change. |
| D-026 | 2026-08-06 | Accepted | Replace the sticky five-beat hero with one normal-flow responsive hero: original tactile panorama, gradient into Midnight, two-column dream/mechanism copy, and an overlapping trust panel. | Requiring repeated scroll gestures before the product contract adds friction. The single scene preserves the night-shift metaphor while making the promise, mechanism, actions, and trust boundary legible in one viewport-scale composition. | Measured comprehension or conversion tests show a different first-screen structure performs better without hiding the product contract. |
| D-027 | 2026-08-11 | Completed test slice; runtime superseded by D-044 | Prove the Career Vault → immutable revision → single-use approval → synthetic receipt loop with typed deterministic domain functions, deep-validated browser-local sample state, and explicit failure injection. Never store real résumé content, sensitive facts, credentials, or production authority in browser storage. | This created a testable vertical slice before connecting authenticated persistence or external systems. Its deterministic fixtures remain useful after the sample runtime is removed. | Reuse only as test evidence; D-044 owns the candidate runtime. |
| D-028 | 2026-08-11 | Superseded by D-032 | Make the working dashboard the root route and hide unrouted marketing and unfinished product destinations. Expose only Today, Approval Queue, Applications, and Career Vault until additional surfaces have functioning local contracts. | A focused prototype should demonstrate one complete trust loop without sending evaluators into landing-page friction or blueprint dead ends. | Superseded when Queue became the single source-of-truth surface and the destination set was simplified. |
| D-029 | 2026-08-11 | Superseded by D-032 | Make Today a dense application console: compact snapshot cards, a searchable and filterable application table, and detail actions behind each row. Keep rule ratios, synthetic labels, and named approval boundaries instead of copying unsupported match scores, bulk-submit controls, or real-ATS language. | Evaluators need to understand the whole workspace at a glance without scrolling through oversized status cards, while the visual compression must not weaken the product's evidence and authority boundaries. | Superseded when usability direction moved from a dense dashboard console to one lean Queue with detail on demand. |
| D-030 | 2026-08-11 | Superseded by D-032 | Make the default dashboard an audit-led activity feed with deterministic night-shift replay, timestamped high-level actions, and a pulse that activates only during replay or a real local in-progress state. Keep the dense application table under Applications and label the experience as sample data with no ATS connected. | The activity feed makes continuous work legible and engaging while preserving a single source of truth and avoiding an unsupported claim that the prototype is connected to live job or employer systems. | Superseded after Queue proved clearer as the source-of-truth surface. |
| D-031 | 2026-08-11 | Accepted | Make onboarding progressive and value-first: parse the resume in parallel, verify minimal facts and search rules, then show a sourced match before asking for a street address, portal credential, or application-specific sensitive answer. Do not collect voluntary protected answers globally, store one reusable cross-ATS password, offer evidence-free rewriting, or allow bulk submission in the MVP. Start in draft-only mode with one single-use approval per immutable application. | The authenticated Tsenta walkthrough demonstrates strong progress design but a trust-heavy six-step wall before first dashboard value. RoleDawn can reduce setup friction and credential blast radius while preserving operational readiness through just-in-time verified facts, origin-scoped secrets, and precise human gates. | Form benchmarks or candidate research prove a particular field must move earlier; the evidence, credential-isolation, and single-use approval invariants remain. |
| D-032 | 2026-08-11 | Superseded by D-044 | Make Queue the default authenticated surface and merge the earlier Today, Approval Queue, Applications, and Tracker concepts into one status list. Keep Swipe and Career Vault as the only other primary destinations; use Add a job as a contextual dialog and keep activity inside application detail. This supersedes D-028 through D-030. | Queue remains the source-of-truth interface, but the prototype-only destinations were removed from runtime by D-044. | Revisit additional destinations only after persistent pasted-link preparation works end to end. |
| D-033 | 2026-08-11 | Superseded by D-044 | Add **Browse Jobs** as a fourth primary destination between Queue and Swipe. Browse supports deliberate search, filtering, saving, detail review, and adding one job to the queue; Swipe remains the fast one-job-at-a-time path. Saving a job never creates an application, and adding one starts preparation only. This supersedes the three-destination clause in D-032 without changing Queue's authority. | The concept remains available for later research, but fixture-backed discovery no longer belongs in the persistent runtime. | Restore only with a durable attributable catalog and measured user need. |
| D-034 | 2026-08-11 | Accepted | Sort Queue by immutable `queued_at` descending with an opaque internal tie-breaker. Hide requisition, application, revision, approval, attempt, workflow, and provider IDs from ordinary candidate copy while retaining them in the domain for routing, deduplication, approval, idempotency, and audit. Remove redundant Queue eyebrow copy. | Candidates scan for company, role, status, time, and the next human action. Internal identifiers add noise and `updated_at` sorting makes rows jump while agents work. | Research shows a specific provider reference materially helps support or candidate trust; expose it only in contextual evidence, never as a primary label. |
| D-035 | 2026-08-11 | Superseded by D-044 | Keep Queue, Browse Jobs, Swipe, and Career Vault as primary navigation; make Application Workspace a routed detail and Settings a secondary utility grouped into Search rules, Application behavior, Notifications, and Account and data. Map every surface to one modular typed API/control plane rather than creating a service per screen. | The modular control-plane rule remains; the multi-destination runtime was removed so the persistent application loop can be completed first. | Restore a destination only when its persistent read/write contract exists. |
| D-036 | 2026-08-11 | Accepted | Build a deterministic application system with bounded agentic specialists: PostgreSQL owns durable domain truth, Temporal owns long-running execution, and model/agent runtimes operate only inside typed activities with fixed tools, limits, and schemas. | Research, evidence selection, drafting, and unfamiliar forms require judgment; identity, retries, approval, submit, and confirmation require replayable authority. This preserves agent flexibility without creating a free-running control plane. | A tested alternative preserves the same state ownership, human waits, replay, policy, at-most-once submit, and recovery guarantees with materially lower complexity. |
| D-037 | 2026-08-11 | Accepted | Build one shared attributable job catalog and model candidate eligibility, fit, decisions, applications, and outcomes as separate versioned relations. Fetch each employer source once, create an application only when a candidate queues a job, and never store user behavior directly on canonical jobs. | Public ATS feeds are employer-scoped, while candidate relationships have independent meanings and lifecycles. Separating them improves cost, deduplication, privacy, matching evaluation, and UI clarity. | A licensed provider contract materially changes catalog ownership or an observed scale constraint requires another projection while preserving canonical identities and relationship semantics. |
| D-038 | 2026-08-11 | Accepted | Productize the current application, resume, cover-letter, and no-slop workflows as immutable task policies, schemas, validators, renderers, and evaluation cases—not mutable per-user production skill files. Offer `AS_UPLOADED`, `REORDER_AND_TIGHTEN`, and `REWRITE_FROM_VERIFIED_FACTS`; no mode relaxes the truth gate. | The existing workflows contain strong quality behavior, but production needs replayable versions, provenance, claim validation, and testable bounds. “Aggressive” wording can imply fabrication even when the intended behavior is strong truthful tailoring. | Candidate research proves different labels are clearer; the evidence and claim constraints remain. |
| D-039 | 2026-08-11 | Supabase clause superseded by D-041; browser benchmark open | Benchmark Supabase Postgres/Auth for the first data foundation and Browserbase + Playwright for browser infrastructure, with Stagehand as constrained DOM fallback, Orgo as desktop-only fallback, and Browser Use Cloud as the second browser benchmark. | D-041 adopted Supabase as the bounded first control plane. The browser-provider benchmark remains open under O-002. | O-002 selects a browser provider, or D-041's reversal trigger fires. |
| D-040 | 2026-08-11 | Recommended | Build the pasted-link application engine as the first backend vertical slice before broad automated job ingestion: one official URL resolves to a canonical job version, versioned preparation modules produce one immutable application packet, a brokered disposable computer fills the draft, one precise approval releases at most one submit attempt, the same attempt is reconciled, and the session is safely destroyed. Add Cua Sandbox to O-002 behind `BrowserSessionBroker`; do not select it without the benchmark. | This productizes the founder's proven Codex-like workflow and tests the hardest value and trust boundaries without making catalog breadth, Swipe, iMessage, or a permanent agent prerequisites. The provider-neutral broker keeps Cua capability claims from becoming architecture decisions. | Pasted-link usage fails to predict paid value, another slice proves the core loop faster, or the 100-form browser benchmark selects a materially better execution shape. |
| D-041 | 2026-08-12 | Accepted | Adopt the Supabase-backed **HireWire** control plane as RoleDawn's first production authority for Auth-linked tenancy, PostgreSQL domain state, private Storage, RLS-scoped reads, transactional RPC commands, semantic events, and the outbox. Build the runtime in stages: authenticated reads and pasted-link intake; immutable packet preparation; brokered browser shadow mode; then controlled submit and reconciliation. Supabase does not own workflow, model, browser, or policy semantics, and privileged credentials remain server/worker only. | Four ordered migrations now encode identity and tenancy, the shared job catalog, application runtime, Career Vault, immutable packets, single-use approvals, attempts, receipts, and private buckets. Accepting this bounded platform choice is the shortest path from browser-local fixtures to one durable source of truth while preserving owned adapters and deterministic side-effect gates. | Hosted RLS/security review, migration operations, measured cost/scale, or portability requirements fail; or a later workflow engine changes coordination without changing PostgreSQL domain authority. |
| D-042 | 2026-08-12 | Accepted for founder dogfood | Make the first executable worker a leased, one-shot consumer of `application.queued`. It resolves only official hosted Greenhouse, Lever, and Ashby links through fixed public API origins, versions the posting, and advances the persisted intake. Unsupported links fail safe. Broad catalog polling is not a prerequisite. Preparation, computer fill, and submit remain later commands with separate authority; founder dogfood stops before the final employer submit action. | This is the shortest honest bridge from the durable Queue to one real posting while preserving replay, bounded network access, provider adapters, and a clean handoff to packet generation. It exposes runtime and data-contract failures before adding model or computer-use uncertainty. | Direct-link coverage is too low for the initial role family, the official APIs change, or measured founder runs show a different source/provider reaches a reviewable packet faster without weakening the same safety boundary. |
| D-043 | 2026-08-12 | Partially superseded by D-044 | Keep the instant synthetic workspace and persistent Supabase candidate as separate test modes. The database shortcut may bypass email delivery only: it must be development-only, loopback-only, fixed-account, secret-gated, and must establish a normal Supabase session before identity-derived bootstrap and RLS-scoped reads. Never fake an actor or use the service role for ordinary dashboard access. | D-044 removes the synthetic runtime. The fail-closed database-test candidate remains valid because it establishes a normal session and exercises `auth.uid()` and RLS. | Supabase provides a safer first-party local test mechanism or the shortcut cannot preserve the production trust boundary. |
| D-044 | 2026-08-12 | Accepted | Cut the executable product to a persistent-only path: `/` redirects to authenticated `/dashboard`; runtime surfaces are Queue, pasted-link intake, and database-backed application detail. Remove Browse, Swipe, Career Vault fixtures, the landing page, and the browser-local sample workspace. Keep only explicit test-support adapters that cannot enter the runtime dependency graph. | One honest durable loop is more valuable than polished fixture breadth. A single persistence model prevents evaluators and engineers from mistaking browser-local state for account data and concentrates work on Auth, RLS, intake, preparation, and recovery. | Restore a surface only when it has an accepted persistent contract, real provenance, and end-to-end evidence without weakening the Queue or approval boundaries. |
| D-045 | 2026-08-12 | Accepted | Mark Milestone 0 complete on hosted run `20260812135034`. Treat the 11 forward migration versions through `20260812134739` as the accepted HireWire development baseline. The accepted scope is Auth, RLS, durable intake/read models, outbox recovery, and one official-source resolution; it does not include application preparation or execution. | The preserved run passed two-user isolation, replay and mismatch controls, canonical-URL deduplication, application reads, bounded dead-letter recovery, a resolver claim/completion, and cleanup. This is enough evidence to move the build gate to reviewed candidate evidence without implying an employer-side capability. | Any change to Auth, tenancy, command functions, Queue/detail reads, catalog identity, worker leasing/recovery, or the hosted migration baseline requires the same gated acceptance run again. |
| D-046 | 2026-08-12 | Accepted | Keep catalog persistence replay-safe across worker retries: on a canonical-job uniqueness conflict, reuse the existing job only when its source listing matches; use one normalized observation timestamp for catalog writes; and translate ingestion `UNSPECIFIED` to database `UNKNOWN` at the persistence boundary. | These rules prevent a retry from creating a second catalog identity, reject a conflicting source identity, avoid timestamp drift across one observation, and keep provider-neutral normalization separate from the database enum. | Revisit only if catalog identity or the database vocabulary changes while preserving equivalent conflict, provenance, and replay guarantees. |

## Open decisions

| ID | Decision needed | Evidence to collect | Owner |
|---|---|---|---|
| O-001 | Exact launch pricing and caps | Cost per attempted/confirmed application; support minutes; three willingness-to-pay tests | Founder |
| O-002 | Managed browser vendor | 100-form benchmark across price, persistence, CAPTCHA handoff, observability, and geographic coverage | Engineering |
| O-003 | Hosted Supabase project shape and launch gate after D-041 | Region, plan, remote migration/RLS tests, backup and recovery controls, cost, and portability review | Engineering |
| O-004 | Photon production readiness | Written answers on Apple authorization, DPA, subprocessors, retention, number portability, suspension, SLA, incidents | Founder/security |
| O-005 | Dedicated job-search inbox | Candidate research, deliverability, employer-visible identity, OAuth review burden, and forwarding behavior | Product/security |
| O-006 | RoleDawn final clearance | Counsel-led trademark search, domain purchase, handles, linguistic and recall tests | Founder/legal |
| O-007 | First role family | Interview demand, form regularity, job volume, willingness to pay, and outcome measurability | Product/GTM |
| O-008 | Primary model provider and task routes | Blind evals on schema validity, factual support, abstention, tool fidelity, accepted-output cost, and latency | Applied AI/engineering |
| O-009 | OAuth/token broker buy-versus-build | Threat model, incident controls, tenant isolation, provider coverage, DPA, export, revocation, and total operating cost | Security/engineering |
| O-010 | Licensed job-data partner after direct ATS coverage | Contract rights, freshness, duplicates, full descriptions, source/apply URLs, target-role coverage, attribution, and cost in an Adzuna/Lightcast/LinkUp bakeoff | Product/data/legal |
| O-011 | Company-research provider and freshness policy | Citation quality, source coverage, date accuracy, cacheability, conflicts, accepted-output quality, latency, and cost | Applied AI/research |
